---
title: "FastAPI 教程 - 07. 路径参数校验"
date: "2025-11-30"
category: "FastAPI"
tags: ["FastAPI", "校验", "Path", "数值校验"]
author: "Devliang24"
description: "学习使用 Path 类对路径参数进行数值范围校验（大于/小于）和格式限制。"
---

# FastAPI 教程 - 07. 路径参数校验

> **适合人群**：Web 开发者
> **前置知识**：路径参数基础
> **预计时间**：10 分钟

## 🎯 引入 Path

与 `Query` 类似，FastAPI 提供了 `Path` 用于校验路径参数。

```python
from fastapi import FastAPI, Path

app = FastAPI()

@app.get("/items/{item_id}")
async def read_items(
    item_id: int = Path(title="The ID of the item to get"),
    q: str | None = None
):
    results = {"item_id": item_id}
    if q:
        results.update({"q": q})
    return results
```

⚠️ **注意**：路径参数总是**必填**的，所以你不需要声明它为必填。即便你设置了默认值，它也不会生效（但可以用来存放元数据）。

## 🔢 数值校验

对于数字类型的参数（如 ID），我们经常需要限制其范围。

```python
@app.get("/items/{item_id}")
async def read_items(
    item_id: int = Path(..., title="Item ID", ge=1, le=1000),
    q: str | None = None
):
    return {"item_id": item_id}
```

常用的数值校验参数：

*   `gt`: 大于 (greater than)
*   `ge`: 大于等于 (greater than or equal)
*   `lt`: 小于 (less than)
*   `le`: 小于等于 (less than or equal)

例如 `ge=1` 意味着 `item_id` 必须大于或等于 1。

## 🔄 参数顺序技巧

如果你的函数同时包含路径参数、查询参数和请求体，FastAPI 足够智能，可以区分它们。

但是，如果你有默认值的参数放在了无默认值的参数前面，Python 解释器会报错。为了解决这个问题，你可以把所有参数都显式地使用 `*` 作为第一个参数：

```python
async def read_items(
    *, 
    item_id: int = Path(..., title="The ID"), 
    q: str
):
    ...
```

这里的 `*` 告诉 Python，后续的所有参数都必须以关键字参数的形式调用（FastAPI 会自动处理这部分）。

## 📚 总结

*   使用 `Path` 对路径参数进行校验。
*   路径参数总是必填的。
*   使用 `gt`, `ge`, `lt`, `le` 进行数值范围控制。

下一章，我们将学习如何同时处理**路径、查询和请求体**参数。
