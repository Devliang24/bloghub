
# FastAPI 教程 - 01. 简介与安装

> **适合人群**：Python Web 开发初学者
> **前置知识**：Python 3.8+ 基础
> **预计时间**：10 分钟

## 🚀 什么是 FastAPI？

**FastAPI** 是一个用于构建 API 的现代、快速（高性能）的 Web 框架，使用 Python 3.8+ 并基于标准的 Python 类型提示。

### 核心特性

*   **⚡ 极快**：可与 NodeJS 和 Go 比肩的极高性能（归功于 Starlette 和 Pydantic）。
*   **⚡ 快速编码**：将功能开发速度提升约 200% 至 300%。
*   **🐛 更少 Bug**：减少约 40% 的人为（开发者）错误。
*   **💡 智能**：极佳的编辑器支持。处处皆可自动补全，减少调试时间。
*   **📝 简单**：设计易于使用和学习，阅读文档的时间更短。
*   **📉 简短**：代码重复最小化。通过不同的参数声明实现丰富功能。
*   **🏗 健壮**：生产可用级别的代码。还有自动生成的交互式文档。
*   **📜 标准化**：基于并完全兼容 API 的开放标准：OpenAPI（以前称为 Swagger）和 JSON Schema。

## 🛠️ 环境搭建

我们将使用 **UV** 包管理器来管理我们的项目和依赖，它比传统的 pip 快得多。

### 1. 创建项目目录

```bash
mkdir fastapi-learning
cd fastapi-learning
```

### 2. 初始化 UV 项目

```bash
uv init
```

### 3. 安装 FastAPI

安装 FastAPI 以及 `standard` 扩展（包含 `uvicorn` 等生产环境所需的标准依赖）。

```bash
uv add "fastapi[standard]"
```

> **💡 提示**：如果你在国内，建议配置清华源以加速下载：
> ```bash
> echo 'index-url = "https://pypi.tuna.tsinghua.edu.cn/simple/"' > uv.toml
> ```

## ✅ 验证安装

创建一个名为 `main.py` 的文件，写入以下测试代码：

```python
from fastapi import FastAPI

app = FastAPI()

@app.get("/")
async def root():
    return {"message": "Hello World"}
```

运行开发服务器：

```bash
uv run fastapi dev main.py
```

如果看到类似以下的输出，说明安装成功！

```
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:     Started reloader process [12345] using WatchFiles
INFO:     Started server process [12346]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

## 📚 下一步

现在你已经成功安装并运行了 FastAPI！在下一章中，我们将深入了解如何构建我们的第一个 API 接口。
